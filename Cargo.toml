[package]
name = "scaffold"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
colored = "3.1"
dark-light = "2.0"
portable-pty = "0.9.0"

[profile.dev]
debug = 0

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
